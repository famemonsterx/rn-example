import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    header: {
        display: 'flex',
        flexDirection: 'row',
        marginTop: 20,
        justifyContent: 'center',
        alignItems: 'center'
    },
    logo: {
        resizeMode: 'contain',
        width: 150,
        height: 50
    }
});