import React from 'react'
import {View,Text} from 'react-native'

const Modal = ({ label }) => {
    return (
        <View>
            <Text>
                {label}
            </Text>
        </View>
    )
};

export default Modal